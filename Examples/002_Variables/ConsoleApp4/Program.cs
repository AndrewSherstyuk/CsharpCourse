using System;

namespace ConsoleApp4
{
    class Program
    {
        static void Main()
        {
            //Создание переменных без предварительной инициализации (предварительного присвоения значения)
            string string1;
            bool bool1;
            byte byte1;
            int int1;
            double double1;

            //Раскомментировать
            //Ошибка при попытке использования неинициализированной локальной переменной!
            //Запрещается использования 

            /*
            Console.WriteLine(string1);
            Console.WriteLine(bool1);
            Console.WriteLine(byte1);
            Console.WriteLine(int1);
            Console.WriteLine(double1);
            */

        }
    }
}
